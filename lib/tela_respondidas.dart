import 'package:flutter/material.dart';

class TelaRespondidas extends StatelessWidget {

  const TelaRespondidas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Respondidas'),
      ),
      body: const Placeholder(),
    );
  }
}